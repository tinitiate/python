# 1. write a function for loan engine
#    to the function pass the master data, customer data
# Output, print dictionary with these keys 
# Approved: {"CustName":, "CustCS":,CustLoanAmt":, "LoanApplicationStatus":, "InterestRate":,"DurationInMonths":}
# Denied: {"CustName":, "CustCS":,CustLoanAmt":, "LoanApplicationStatus":}


"""
# bulk testing
import random
import string
 
# print result
for c1 in range(10000000):
    cust_name = ''.join(random.choices(string.ascii_uppercase +
                       string.digits, k=4))
    cust_cs = random.randint(100, 505)
    cust_loan_amt = random.randint(10000, 23005)
    print(cust_name,cust_cs,cust_loan_amt)
    # loan_calc(cust_cs = ,cust_loan_amt=)
"""